# Crafdy-mobile Claude Code Instructions

## プロジェクトの目的
Crafdy は、建設業の職人・施工店向け AI 業務アシスタントです。
主役は main-chat です。
日報、経費、請求書、見積、現場管理を、チャット起点で自然に進められる体験を目指します。

この repo で最優先することは、
- 初見で何ができるか分かる
- 次に何をすればいいか分かる
- 難しそうに見えない
- main-chat が主役
- role-aware で dead-end を作らない
です。

---

## 絶対に守るUI原則
以下は常に優先してください。

1. 3秒で分かる
- 画面を開いた瞬間に、何ができるか・何をすればいいかが分かること

2. 1画面1目的
- 1つの画面に複数の目的を詰め込みすぎないこと

3. 動詞で始まるCTA
- ボタンや導線は「何が起きるか」がすぐ分かる文言にすること

4. role-aware
- owner / office / member / unassigned で見せるものを整理すること
- 役割に不要なものを前面に出さないこと

5. 「閲覧できません」で終わらせない
- 制限がある場合でも、次に何をすればいいかを必ず添えること

6. main-chat を主役にする
- 高頻度行動は main-chat 側に寄せる
- menu は低頻度だけど必要な導線の置き場にする

7. 新機能追加より文言 / 導線 / 空状態改善を優先
- まずは使い方の理解、第一印象、詰まりの除去を優先すること

---

## 実装判断の優先順位
実装で迷ったら、以下の順番で判断してください。

1. 既存業務ロジックを壊さない
2. main-chat 主役を守る
3. Figma 正本の見た目に寄せる
4. 文言・余白・視線誘導を整える
5. 新しい機能追加は最後

---

## Figma / デザイン正本の扱い
UI デザインは自然言語の改善提案ではなく、Figma 正本準拠で進めます。

原則:
- Figma Make 生成物 repo を正本とする
- 改善提案を足さない
- まずは「同じ絵」を出すことを優先する
- Web 実装コードをそのまま使わず、React Native に読み替えて移植する
- 見た目再現と業務ロジック統合を同時にやりすぎない

デザイン正本 repo:
- Crafdymainchat
- branch: main

最重要参照ファイル:
- src/app/App.tsx

参考スタイル:
- src/styles/theme.css
- src/styles/index.css
- defaultshadcn_theme.css

---

## 実装の進め方
UI 実装は以下の方針で進めること。

### Phase 1: 純UI再現
- Figma 正本の見た目再現を優先
- 必要なら新規コンポーネントへ切り出してよい
- dummy state / fixed state を使ってもよい
- role-aware や実データ分岐に引っ張られないこと

### Phase 2: 既存ロジック接続
- 見た目が合ったあとで既存ロジックと繋ぐ
- owner / member / unassigned / 現場未選択などの実状態を戻す
- ただし Phase 1 完了前に混ぜすぎない

---

## コンポーネント分離の考え方
main-chat.tsx に全部継ぎ足さないこと。
必要なら見た目責務で分離してよい。

例:
- MainChatHomeView
- MainChatAttachSheetView
- MainChatDrawerView
- MainChatThemeSheetView

原則:
- まず純UI
- 次に接続
- 一度に全部やらない

---

## main-chat の情報設計
### ホーム上部
- 左上: ハンバーガー
- 中央: Crafdy
- 右上: 現場未選択なら「現場を選ぶ」、選択済みなら「現場切り替え」

### ウェルカム文言
- 名前あり:
  - 「〇〇さん、お疲れさまです。今日は何を手伝いましょうか？」
- 名前なし:
  - 「お疲れさまです。今日は何を手伝いましょうか？」

### 補助文
- 「日報・経費・請求・見積を、チャットでそのまま進められます。」

### prompt chips
- 日報を入力
- 経費を登録
- 請求書を作る
- 見積を作る
- 何ができる？
- 最初に設定は？

### 入力欄
- 画面の主役
- pill 型
- 左に `+`
- 右に送信 or マイク
- chips のすぐ下で視界に入る位置

### `+` アクション
`+` は添付系だけに寄せること。
- 写真を追加
- カメラで撮る
- ファイルを添付
- レシートを追加

`+` に入れないもの:
- 現場を作る
- 現場を選ぶ
- 現場を切り替える

### drawer menu
drawer は低頻度だけど必要なナビの置き場。

owner / office の基本構成:
#### 仕事
- ダッシュボード
- 現場一覧 / 切り替え
- 請求書履歴
- 見積履歴

#### チーム
- メンバーを招待

#### 設定
- 会社情報
- 請求書テンプレ
- 契約・プラン
- 表示テーマ

#### その他
- アカウント
- ヘルプ
- ログアウト

member は見せすぎないこと。
unassigned はさらに絞ること。

---

## テーマ切替
テーマは以下の3択にする。
- ライト
- ダーク
- 端末に合わせる

原則:
- ダークは深いネイビー基調
- ライトは白反転ではなく、少し青みやグレーを含む上品な明るさ
- 両方とも同じブランドに見えること

---

## 変更の粒度
原則:
- 1PR = 1責務
- 一度に広げすぎない
- まず小さく通す
- UI刷新は分割して進める

例:
- home
- attach sheet
- drawer
- theme switch

---

## commit / push ルール
- ユーザーが commit を明示するまでは commit しない
- ユーザーが push を明示するまでは push しない
- commit と push は必ず分ける
- commit 前に差分範囲を明確にする
- push 前に status と最新 commit を確認する

絶対に守ること:
- `.claude/settings.local.json` は commit に含めない
- 今回の変更と無関係なファイルを巻き込まない

---

## 返却ルール
差分返却時は、以下を基本とする。

1. git status --porcelain
2. git diff --stat
3. git diff -- <対象ファイル>
4. 変更要約
   - 何を変えたか
   - 何を参照したか
   - どこをそのまま再現したか
   - React Native 向けにどう読み替えたか
   - まだ未着手の部分は何か
5. commit / push はしていないことを明記

返却では、
- 改善提案を勝手に増やさない
- 今回触った範囲を正直に返す
- 推測で断定しない

---

## 禁止事項
- Figma 正本を見ずに「なんとなく整える」
- role-aware を理由に主役 UI を崩す
- 1ファイルに全部継ぎ足して見た目を破綻させる
- 既存の業務ロジックを勝手に再設計する
- ユーザー未承認で commit / push する
- `.claude/settings.local.json` を commit する

---

## このプロジェクトでの成功条件
成功とは、
- main-chat が主役に見える
- 初見でも「何ができるか」「何をしていいか」が分かる
- 見た目が上品で信頼感がある
- owner / member / unassigned で詰まらない
- Figma 正本の意図を壊さず React Native に落とせている
ことです。

---

## エディタ設定
- `Cmd+Esc` (Mac) でチャットパネルを開く
- `Cmd+Option+K` でファイルを Claude Code に送信
- 対話言語: 日本語
